Description:
    Creates a new CPUI collection in an existing module

Example:
    yo cpui:collection [module name] [model name]

    This will create:
      * app/assets/scripts/[module]/collections/[model].coffee
      * spec/js/[module]/collections/[model].coffee

